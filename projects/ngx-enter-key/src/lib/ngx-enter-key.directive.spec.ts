import { NgxEnterKeyDirective } from './ngx-enter-key.directive';

describe('NgxEnterKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxEnterKeyDirective();
    expect(directive).toBeTruthy();
  });
});
